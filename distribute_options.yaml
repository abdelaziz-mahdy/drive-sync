output: dist/

releases:
  - name: macos-release
    jobs:
      - name: macos-dmg
        package:
          platform: macos
          target: dmg

  - name: windows-release
    jobs:
      - name: windows-exe
        package:
          platform: windows
          target: exe

  - name: linux-release
    jobs:
      - name: linux-deb
        package:
          platform: linux
          target: deb
